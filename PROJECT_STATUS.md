# PROJECT_STATUS.md

## Current Work Item
- None — repository is idle, awaiting the next assignment.

## Current Stage
- Idle.

## Change Classification
- Change Type: N/A
- Risk Level: N/A
- Code Change Required: N/A
- Architecture Change Required: N/A
- Security Review Required: N/A

## Completed
- Blank-template reset completed through PR #205 (`aa2a871`); historical records remain recoverable from Git history.
- Issue #208 — `reset-to-template` no longer destroys the decision log, and `adr-audit` fails closed when the ADR count drops. Merged via PR #209 (squash) as `e820389`. Independent QA passed at `31b0b13` (518/518, mutation-verified). ADR-0017 and ADR-0019 restored to `DECISIONS.md`; the remaining 16 are recoverable via the command recorded there, by explicit Human Maintainer decision. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/208#issuecomment-5551564975
- Issue #210 — GitLab CI now runs the same portable validators as GitHub CI, enforced by `scripts/validate-ci-parity.mjs` wired into both CI files. Merged via PR #211 (squash) as `be15d7b`. Independent QA ran three rounds (503 → 517 tests) before passing: round 1 found a committed `node_modules` symlink and three ways the parity detector could be defeated; round 2 found a fail-open where a renamed GitHub job silently disabled the check; round 3 (continued past the stated 2-round rework ceiling by explicit Human Maintainer decision) found the same guard didn't cover a job restructured to yield zero commands. Final round passed with one non-blocking Minor and one design Question left open below. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/210#issuecomment-5551566808
- Issue #214 — `reset-to-template` no longer destroys the risk register, and `validate-risk-register` fails closed when the total risk-entry count drops relative to a prior commit (merge-base + branch-walk comparison, mirroring `adr-audit.mjs`'s `comparisonRefs`). `RISKS.md` itself is unmodified: AC-01 investigation found no currently open Issue cites a specific `RISKS.md` id, so no historical content was restored, per explicit instruction that this data-restoration scope question is a Human Maintainer decision, not a Developer Agent guess. Merged via PR #216 (squash) as `b44c057`. Independent QA passed at `6064154` (542/542, mutation-verified: comparison-walk logic, regression check, and reset guard each individually confirmed load-bearing by deletion) with one non-blocking Question left open below. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/214#issuecomment-5551998929
- Issue #215 — `scripts/repin-source-matrix.mjs` (new, `npm run repin:source-matrix`) recomputes and rewrites stale sha256 entries in `test/fixtures/context-pack-v1/required-source-matrix.json`, closing the tooling gap where editing any of the 25 pinned canonical/skill files broke 7 tests with no way to fix it but a hand-computed hash edit. Reuses the existing `sha256(bytes)` helper (newly exported) from `scripts/lib/context-compatibility-v1.mjs` for hash-computation parity with the validator; updates every occurrence of a redundantly-pinned path; fails closed on pre-existing hash inconsistency or format drift; byte-identical no-op when nothing changed. Merged via PR #217 (squash) as `e4bc02b`. Independent QA passed at `ea6767b` (551/551 after merging in Issue #214's closeout, all three load-bearing claims mutation-verified) with two non-blocking Minors on test-quality precision left open below. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/215#issuecomment-5552109777
- Issue #220 — closed the two test-coverage gaps QA left open from #214 and #215, test files only, no production code changed. Added a test locking in `validate-risk-register.mjs`'s intentional fail-open behavior when a comparison commit is unreadable (the same precedent already established in `adr-audit.mjs`); strengthened `repin-source-matrix.mjs`'s round-trip-guard test (its fixture previously had an already-correct hash, so it never actually proved the guard prevents a write); removed a duplicate test with no unique discriminating power. Merged via PR #221 (squash) as `f1eb57d`. Independent QA passed at `e257893` (551/551 net zero, all claims mutation-verified, one dropped-conjunct mutation confirmed as a genuine equivalent mutant rather than a coverage gap). Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/220#issuecomment-5552400432
- Issue #224 — recorded two Human Maintainer decisions, no code change: (1) `RISKS.md`'s 7 historical entries destroyed by earlier resets will not be reconstructed, since no currently open Issue cites any of them by id; (2) PR #204's stale `post-merge-closeout` label removed. Merged via PR #223 (squash) as `6fc6828`. Verified at `0be8f85` (label removal confirmed empty via the Issues API, `validate:contracts` and `validate:project-state` PASS). Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/224#issuecomment-5571744929
- Issue #226 — recorded ADR-0020, no code change: accepted SA Agent's recommendation to keep `validate-ci-parity.mjs`'s `githubJobCommands` zero-command guard as-is, closing Issue #210's round-3 design Question; deferred the separately-surfaced `normaliseCommand` multi-line finding (confirmed latent, not currently live). Merged via PR #227 (squash) as `9889da6`. Verified at `f296949` (`validate:contracts`, `adr:audit` at 3.67:1, `validate:project-state` all PASS). Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/226#issuecomment-5571886940
- Issue #229 — recorded ADR-0021, no code change: accepted Option B (body-below-frontmatter parity, `name:` pinned equal) for Issue #212 (IMP-006)'s AC-01, over Option A (rejected) and Option C (deferred — the only option that would also catch adapter-vs-canonical drift, not justified for a single-user setup now); approved relocating `role-definitions.md`'s Terminal Dispatch section into `task-execution-mode.md` for AC-05 headroom, declined the optional QA Cross-Platform Acceptance Criteria Gate relocation. Merged via PR #230 (squash) as `6a9d278`. Verified at `56c45e8` (`validate:contracts`, `adr:audit` at 3.00:1, `validate:project-state` all PASS). Issue #212 itself updated with `status:spec-ready` and AC-01 checked off; implementation (AC-02–AC-04, AC-06–AC-08) not yet dispatched. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/229#issuecomment-5572301977
- Issue #212 (IMP-006) — implemented AC-02 through AC-06 per ADR-0021's Option B. New `scripts/validate-adapter-parity.mjs` (`npm run validate:adapter-parity`) compares role-adapter bodies below frontmatter across `.claude/agents/`, `.agents/agents/`, `.agent/agents/` (both new trees seeded, 11 files each); `name:` pinned equal across all three trees while `description`/`tools` may differ per host; fails closed on a missing adapter. Wired into both `.github/workflows/validate-contracts.yml` and `.gitlab-ci.yml`, confirmed by `validate:ci-parity`. `role-definitions.md`'s Terminal Dispatch and Boss Visibility section relocated into `task-execution-mode.md`, recovering context-budget headroom from 29985/30000 to 29384/30000 (616 headroom, matching ADR-0021's estimate within 1 token); `repin:source-matrix` run. 5 previously-unwired adapters (`ba-agent`, `config-agent`, `documentation-agent`, `orchestrator-agent`, `pm-agent`) wired to their existing skills via a new `## Skill Routing` section, mirrored across all three trees. Merged via PR #232 (squash) as `555484e`. Independent QA passed at `1372e95` (565/565, all AC-02/03/05 claims independently re-derived — drift, name-mismatch, and missing-file scenarios each individually reproduced, not accepted from the Developer's self-review; two Developer-self-flagged items confirmed pre-existing and unrelated). AC-06 remains open (`release-agent` has no skill, correctly gated on AC-07); AC-01/AC-07/AC-08 out of this diff's scope by design. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/212#issuecomment-5572867510
- Issue #212 (IMP-006) — **closed in full.** AC-07: new `release-readiness-checklist` skill (all three trees) distilling `role-definitions.md`'s existing Release Agent section (Versioning and Changelog Contract, Release Evidence Checklist, Triple Rollback Confirmation, Deployment Strategy Statement), per SA Agent's evaluation and Human Maintainer approval (2026-09-07) — Release Agent now matches the Config/Documentation Agent precedent of owning a discoverable skill despite already embedding its policy content elsewhere. New `SKILL_CATALOG.md` row; `## Skill Routing` added to `release-agent.md` across all three trees; `ROLE_SOURCE_CONTRACT['Release Agent']` corrected from its `documentation-closeout` stand-in; `required-source-matrix.json` updated and repinned. Context budget: 29384/30000 → 29534/30000 (466 headroom). Merged via PR #234 (squash) as `f5ff42c`. Independent QA passed at `fed6afb` (565/565; skill-parity 39/39; adapter-parity 11/11; sentence-by-sentence distillation-accuracy check confirmed no invented policy, two non-blocking Observations noted; full validator suite — 17 commands, several outside the original Verify list — all PASS; confirmed no human gate weakened). Issue #212 closed on GitHub. All 8 ACs (AC-01 via ADR-0021/PR #230; AC-02–AC-06 via PR #232; AC-07–AC-08 via PR #234) complete. Evidence: https://github.com/chakrits/AI-Agent-Workflow/issues/212#issuecomment-5573221861

## In Progress
- None.

## Blockers / Open Questions
- Deferred by Human Maintainer decision (2026-09-07, [ADR-0020](DECISIONS.md), Issue #226): SA Agent's `normaliseCommand`/`IGNORED_COMMAND_RE` finding in `scripts/validate-ci-parity.mjs` (a `run: |` multi-line step's non-global `.match()` only extracts the first command, and a block whose first line matches `IGNORED_COMMAND_RE` is discarded whole) is confirmed latent, not currently live — every `run:` step in `.github/workflows/validate-contracts.yml` is single-line. Deferred rather than fixed now; revisit if a multi-line `run:` block is ever introduced.
- Minor from Issue #210's round-3 review: the "yielded no comparable commands" error message in `scripts/validate-ci-parity.mjs` says "if the job was restructured... into a composite action," which is misleading for a job that legitimately runs only ignored commands (`npm test`/`npm ci`). Cosmetic wording only, not fixed.

## Required Artifacts
- Self-review record (#208): `docs/records/qa/2026-09-05-issue-208-decision-log-preservation-code-review.md`
- Self-review record (#210): `docs/records/qa/2026-09-05-issue-210-ci-parity-code-review.md`
- Self-review record (#214): `docs/records/qa/2026-09-05-issue-214-risk-register-preservation-code-review.md`
- Self-review record (#215): `docs/records/qa/2026-09-05-issue-215-repin-source-matrix-code-review.md`
- Self-review record (#220): `docs/records/qa/2026-09-05-issue-220-test-coverage-gaps-code-review.md`
- Framework assessment: `docs/records/misc/2026-09-05-framework-sdlc-assessment.md`

## Next Quality Gate
- None — awaiting next assignment.

## Recommended Next Agent
- None — awaiting next assignment.

## Notes
- Reset to template baseline by `npm run reset:template`.
