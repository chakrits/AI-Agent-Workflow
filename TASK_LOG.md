# TASK_LOG.md

| Date | Work Item | Agent | Action | Result | Next Agent | Notes |
|---|---|---|---|---|---|---|
| 2026-09-09 | Issue #246 | QA Agent | Independent verification of AC-02 through AC-08 | PASS_WITH_FINDINGS at `8ed21a0` (685/685; base re-derived at 666/666 in an isolated copy). Discarded its own first mutation run after finding the `git archive` workspace was not green — 18 environment failures made every mutant appear killed — and rebuilt it as a full copy with a verified baseline, disclosing this in the evidence. Established that AC-07's "additive" claim holds at the function signature but not at the gate: the CI caller passes `linkedIssueNumber` unconditionally, so 13 of 30 historical PR bodies flip success to failure. Confirmed no self-block, since the workflow loads the module from the default branch. Judged the pre-push asymmetry genuinely different from Issue #236's Finding 4 rather than a reopening. Found five further flag shapes the classifier misses, all pre-existing. Three Majors, none blocking. | Human Maintainer | Next Action: merge approval; then a follow-up Issue against the raw-text/token-aware seam. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-09 | Issue #246 | Developer Agent | Implemented AC-02 through AC-08 | Allowed a declared but not-yet-readable `--body-file` with a warning while keeping fail-closed for genuinely uncheckable bodies; refused relative paths rather than guessing a working directory; declined shell-variable expansion with the reasoning recorded in code and `README.md`; relocated the closing-keyword rule into the shared readiness core so CI enforces it, committed separately from the hook changes. Proved AC-02 by executing the destructive shape rather than by unit test — a `touch` preceding a `gh pr create --body-file <not-yet-written>` in one command string, verified to have run — then proved the hook was live by a shape that must still deny, and reported that refusal rather than routing around it. Found and fixed an eighth refuses-legitimate-work shape unprompted: `-F<path>` with an attached value. Declared a deliberate asymmetry between the two callers and flagged it for review. 666 → 685 tests. | QA Agent | Next Action: independent verification. Skill Used: tdd-implementation (supporting: debugging-discipline, code-review-gate) |
| 2026-09-09 | Issue #246 | Documentation Agent | Recorded Human Maintainer decisions on Issue #246's AC-01 as ADR-0024, no code change | Scoped Issue #236's decision 1 rather than overriding it: fail-closed still governs bodies uncheckable at every point in a command's life, while a declared `--body-file` that will exist at execution time is allowed with a warning. Declined shell-variable expansion, since correct expansion needs a shell that has not started and a wrong guess can pass a bad body. Ordered the closing-keyword rule relocated into `work-item-readiness-refresh.yml` — verified that `.githooks/pre-push` is opt-in and CI carries three of the four extra rules, so ADR-0022's invariant is already violated by code merged in PR #243. Ordered the relative-path false pass fixed by refusing relative paths outright. Dropped AC-06's numeric test floor. | Human Maintainer | Next Action: merge, then dispatch Developer Agent for AC-02 through AC-08. Issue #236's AC-06 onward stays blocked until this lands. Skill Used: documentation-closeout (supporting: git-workflow-and-versioning) |
| 2026-09-09 | Issue #246 | SA Agent | Design evaluation of the PreToolUse body-file timing fork | Reproduced F1 and F2 independently, then proved with a canary experiment that a denial blocks the entire tool call rather than the offending command — correcting the parent's framing that blast radius was a property of F1, when it is a property of the hook point and applies equally to decision 1's endorsed no-flag case. Re-derived what CI actually enforces and found three of the four extra rules already total server-side, leaving the closing-keyword rule originated in `.claude/settings.json` alone, which violates ADR-0022's own invariant. Surfaced an unreported false pass: the hook `cd`s to the project root, so a relative `--body-file` can make it validate one file while `gh` submits another. Named what its own recommendation would not protect against. | Human Maintainer | Next Action: decide F1, F2, the relocation, the false pass, and AC-06's test floor. Skill Used: sa-architecture-design |
| 2026-09-09 | Issue #236 / PR #243 | Documentation Agent | Post-merge closeout for Issue #236's AC-02–AC-05 | PR #243 squash-merged to `main` as `0fa6c30`. `PROJECT_STATUS.md`'s Current Work Item reset to idle with the work recorded under Completed, including the three rework cycles and four QA rounds and what each round found. `CHANGELOG.md` gained an Added entry — unlike the ADR-only rounds, this PR changes behaviour. Blockers updated: Issue #236's remaining ACs with N1 named as the item to address before AC-06/07/08 inherit the same seam, the five Minors carried forward, and new Issue #244 for the flaky `validate-review-gate` test found while landing this PR. Issue #236 deliberately left open. | Human Maintainer | Next Action: decide the next round — Issue #236's AC-06 onward, or Issue #244. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-08 | Issue #236 (IMP-007) | QA Agent | Independent verification, round 4 | PASS_WITH_FINDINGS at `6c70a4b`. Confirmed B1, M2, M17, M3, M4 and M8 closed by applying each mutant to the shipped code rather than trusting test counts; M1 partially closed, with the silent-waiver direction still reachable through 4-space indented blocks, blockquotes, HTML `<code>`/`<pre>`, and spans straddling a newline — classified Minor because README's documented form does not match those shapes and a waiver also requires a forgotten keyword. Verified the fenced-closeout residual is bounded exactly as round 2 accepted, not materially wider. Swept the whole test file for vacuous assertions by mutation and found none beyond the ones cycle 3 fixed. Confirmed exactly one existing test was inverted — the assertion that had encoded B1 as correct behaviour. | Human Maintainer | Next Action: merge approval. Four Minors and one Question carried forward. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-08 | Issue #236 (IMP-007) | Developer Agent | Rework cycle 3, authorised past the two-cycle ceiling | Fixed B1 by guarding suppression on `closed.length === 0`, so a declared partial-progress PR carrying a closing keyword is still told which Issue that keyword will close. Added `stripCodeSpans()` feeding both the marker scan and the closing-keyword scan, closing M1 in both directions. Split `fetchWorkItem()`'s bare catch into a classifier so a non-existent Issue is a body defect while an unreachable host stays the decided degraded mode — verified at the live network seam, not only the pure layer. Killed M17's vacuous `--draft` assertion and pinned M3/M4/M8. Disclosed rather than fixed a residual: `isCloseout` still reads the body unscrubbed. | QA Agent | Next Action: independent verification round 4. Skill Used: tdd-implementation (supporting: mutation-testing, test-quality-discipline) |
| 2026-09-08 | Issue #236 (IMP-007) | Human Maintainer | Confirmed B1 a defect and authorised rework past the ceiling | The implementer had flagged as a deliberate judgment that a valid marker also suppresses a wrong-Issue closing keyword. Reproduced: a body carrying `advances-only: issue-236` plus `Fixes #212` passed, while the tool's own warning told the author the PR closes nothing — it would have closed #212 on merge. Constraint 2 suppresses the requirement that a keyword be present, not the presence of a wrong one, and QA established by grep that this rule exists only in the local validator, so the "CI re-derives it" mitigation did not apply. Cycle 3 authorised, following the Issue #210 precedent. | Developer Agent | Next Action: rework cycle 3. Skill Used: No matching skill — Human Maintainer decision recorded directly on the Issue. |
| 2026-09-08 | Issue #236 (IMP-007) | QA Agent | Independent verification, round 3 | BLOCKED at `60bee19`. Probed twelve marker shapes for a blanket escape hatch and found none, but found B1: a valid marker also suppressed a closing keyword naming a different Issue, so a body could close Issue #212 while its own warning said it closed nothing. Also found the marker parser is neither fence- nor quote-aware in either direction, and that `fetchWorkItem()`'s bare catch turns a 404 into `offline: true`. Posted its evidence, then self-reviewed it, found two probe transcripts quoted incorrectly and one mitigation asserted rather than run, and corrected the comment in place with a visible Correction section. | Human Maintainer | Next Action: decide B1's scope; it turns on how narrowly the marker constraint reads. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-08 | Issue #236 (IMP-007) | Developer Agent | Rework cycle 2 — declared partial-progress PRs | Added the `<!-- advances-only: issue-N -->` marker so a PR that advances a multi-AC Issue without closing it can say so, instead of being refused or forced to open a tracking Issue. The marker must name the linked Issue and is rejected when it names another. Closed round-2 finding N1 with 11 subprocess tests driving the real hook contract over stdin. Established by grep that the closing-keyword rule exists only in this validator and was never enforced by CI, so the change narrows a local-vs-CI gap rather than weakening any rule. | QA Agent | Next Action: independent verification round 3. Skill Used: tdd-implementation (supporting: test-quality-discipline) |
| 2026-09-08 | Issue #236 (IMP-007) | Human Maintainer | Decided how partial-progress PRs are handled | Dogfooding found the new gate refused this Issue's own implementation PR, and running it against real merged bodies showed PRs #232 and #234 would also have been refused: the validator recognised only one class of PR that closes no Issue, the post-merge closeout. Rejected the alternative of keeping the rule and opening a tracking Issue for every increment, as a workflow tax no policy imposes. Chose an explicit marker so the intent is declared rather than forgotten, preserving the Issue #224 protection. | Developer Agent | Next Action: rework cycle 2. Skill Used: No matching skill — Human Maintainer decision recorded directly on the Issue. |
| 2026-09-08 | Issue #236 (IMP-007) | QA Agent | Independent verification of AC-02 through AC-05, round 2 | PASS_WITH_FINDINGS at `8c33e87`. Re-ran mutation against the rewritten lexer rather than reusing round 1's map (31 mutants, 21 killed, 10 survived, all classified). Confirmed all seven round-1 findings genuinely resolved, not symptomatically suppressed. Fuzzed the lexer with 200,000 shell-metacharacter inputs: 0 throws, 0 hangs, worst case 1ms — which showed the implementer's `catch` is dead code and the load-bearing fail-open is elsewhere. Found a second entrance to Finding 2's residual the Developer's report omitted (`changedFilesAgainstMain()` returns `[]` when no merge-base resolves, reachable in a single-branch clone). Caught and corrected its own artifact: closeout PRs appeared refused only because file sets derive from the current branch. Six Minor, one Question, no Blocker or Major. | Human Maintainer | Next Action: merge approval. N1 (`runHookMode()` has no test coverage) should be fixed before AC-06/07/08 inherit the seam. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-08 | Issue #236 (IMP-007) | Developer Agent | Rework cycle 1 after QA round 1 returned BLOCKED | Replaced the Blocker's substring regex with `shellCommandSegments()`, a quote- and heredoc-aware lexer that skips heredoc bodies and treats `$( )` as a boundary rather than skipped content, plus `findPrCreateSegment()` requiring the verb in command position. Fixed all four Majors: closeout authorization now derives from the local diff instead of trusting body text; AC-05 drives the real `buildReadinessCheck()` and pins the `isSameRepository` divergence; the pre-push fail-open was fixed in the shared script so both callers converge; a non-GitHub origin reports out-of-scope rather than a body defect. Killed the three surviving mutants and re-ran two more affected by restructuring. 606 → 633 tests. | QA Agent | Next Action: independent verification round 2. Skill Used: tdd-implementation (supporting: debugging-discipline, code-review-gate) |
| 2026-09-08 | Issue #236 (IMP-007) | QA Agent | Independent verification of AC-02 through AC-05, round 1 | BLOCKED at `b8bb36f`. Found a Blocker the implementer's own tests missed: `isPrCreateCommand()` substring-matched the raw command string, so any text where the create verb followed `;`/`&`/`|` plus whitespace was denied — including heredoc content, since `\s` matches newlines. QA hit it writing its own report, whose markdown tables end lines in `|`. Also found four Majors, ran 21 mutants (18 killed), retrieved PR #238's verbatim rejected body via the `userContentEdits` GraphQL field to test against a body CI actually refused, and fired the hook live to obtain a real deny — raising the `PreToolUse` contract from inference to observation. | Developer Agent | Next Action: rework cycle 1. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-08 | Issue #236 (IMP-007) | Developer Agent | Implemented AC-02 through AC-05 | New `scripts/setup-hooks.mjs` (`npm run setup:hooks`) activating the dormant `.githooks` layer, and `scripts/validate-pr-readiness.mjs` (`npm run validate:pr-readiness`) importing `validateReadiness()` rather than re-implementing it. Wired as a `PreToolUse` hook and `.githooks/pre-push`, both thin callers of the same npm script per ADR-0022's invariant. Found two defects by exercising the wired path that unit tests missed: the hook resolved `package.json` from cwd, so invoking from `/tmp` produced a silent allow that defeated the only blocking gate in the Issue; and merged PR #242's real body was refused because closeout PRs carry no closing keyword by design. 565 → 606 tests. | QA Agent | Next Action: independent verification. Skill Used: tdd-implementation (supporting: verification-before-completion, code-review-gate) |
| 2026-09-08 | Issue #236 (IMP-007) | Human Maintainer | Closed AC-03's three open design holes before implementation | ADR-0022 deliberately left three choices unresolved rather than letting the implementer guess. Decided: fail-closed when the PR body cannot be read (a gate that passes when it cannot see its input is not a gate); degrade rather than fail offline (`.githooks/pre-push` must work without network, as the other 15 validators do); and scope the validator to GitHub, leaving `findLinkedIssueNumber()` untouched since it is live in a working CI check and no GitLab consumer exists. Scoped round 1 to AC-02–AC-05. | Developer Agent | Next Action: implementation. Skill Used: No matching skill — Human Maintainer decision recorded directly on the Issue. |
| 2026-09-08 | Issue #239 / PR #238 · Issue #240 / PR #241 | Documentation Agent | Post-merge closeout for both ADR-recording rounds (ADR-0022 and ADR-0023) | PR #238 squash-merged as `7632e7a` and PR #241 as `eeb896a`; tracking Issues #239 and #240 auto-closed by their `Fixes #N` keywords. `PROJECT_STATUS.md`'s Current Work Item reset to idle with both ADRs recorded under Completed. Blockers rewritten to carry the five items now genuinely open: #236 and #237 design-approved but undispatched (with #236's AC-07 named as the binding prerequisite for #237), ADR-0023's ~38-token margin against its own target, the "Stop Conditions" naming collision, `validate-qa-evidence.mjs`'s missing entry point, and #178's stale scope rule. No `CHANGELOG.md` entry: both PRs record decisions and change no behaviour, following the ADR-0020 (#226) and ADR-0021 (#229) precedent. | Human Maintainer | Next Action: decide whether to dispatch Issue #236's implementation (AC-02 onward); nothing else pending. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-08 | Issue #240 (Issue #237 AC-01) | Documentation Agent | Recorded Human Maintainer decision on Issue #237's AC-01 as ADR-0023, no code change | Approved four relocations (AC-02 conditional on an imperative pointer since it is the one boot→on-demand crossing, AC-03, AC-05 retargeted to `AGENT_OPERATING_MODEL.md#human-approval-gates`, AC-07 conditional on moving the R-001 provenance sentence into the skill first). Rejected AC-04 — grep confirmed three backward-routing rules and the QA-skip prohibition exist only in `AGENTS.md`, so the pointer would have deleted policy — and AC-06, whose principles are cited by number four times in the Boundaries index. Corrected AC-08's row shape to five fields after `context-compatibility-v1.mjs:139` showed one registered skill per role makes `Next Skill / Agent` unreachable if moved. Kept the Boundaries index, recorded as a rejected removal. Re-baselined AC-11 from ≤24,600 to ≤26,300 after measuring the approved set at ~26,262. Added AC-13 (AC-04's safe expand-then-point variant) and AC-14 (five stale non-test consumers). | Human Maintainer | Next Action: merge, then post-merge closeout for both ADR rounds. Implementation of AC-02–AC-14 gated on Issue #236's AC-07. Skill Used: documentation-closeout (supporting: git-workflow-and-versioning) |
| 2026-09-08 | Issue #236 (IMP-007) | Documentation Agent | Recorded Human Maintainer decision on Issue #236's AC-01 as ADR-0022, no code change | Adopted the portable-core/thin-invoker design with the governing invariant that `.claude/settings.json` may only invoke a rule `.githooks/` or CI already enforces, never originate one — grounded in SA Agent's finding that `.agents/` and `.agent/` carry no hook runtime, so `.githooks/` is the only portable enforcement point. Established that enforcement level is determined by hook point rather than chosen (`PostToolUse` fires after the write). Withdrew AC-10's blocking commit gate rather than designing an escape hatch, after verifying `validate-project-state.mjs`'s markers describe normal mid-work state and that commit `3b49fca` would have been refused by it. Re-pointed AC-05 to an input contract and AC-12 to containment. Corrected two counts (15 validators, 6 workflows, not 21 and 7). | Human Maintainer | Next Action: merge, then record ADR-0023 for Issue #237. Implementation of AC-02–AC-12 not yet dispatched. Skill Used: documentation-closeout (supporting: git-workflow-and-versioning) |
| 2026-09-08 | Issue #236 / Issue #237 | SA Agent | Independent design evaluation of both new roadmap workstreams before any implementation | Two SA dispatches ran in parallel under Packet v1, each scoped out of the other's Issue. #236: endorsed the hook-layer design with a sharper invariant, withdrew AC-10, re-pointed AC-05/AC-12, found `scripts/validate-qa-evidence.mjs` unreachable from any entry point. #237: rejected AC-04 (destination does not own the content — three backward-routing rules and the QA-skip prohibition exist only in `AGENTS.md`) and AC-06 (principles cited by number four times in the Boundaries index), disproved the "Stop Conditions exists in three files" premise, corrected three measurements, and established from `context-compatibility-v1.mjs:139` that one registered skill per role makes `Next Skill / Agent` unmovable from the catalog. Parent independently re-derived every load-bearing claim before accepting. | Human Maintainer | Next Action: record ADR-0022 and ADR-0023 sequentially. Skill Used: sa-architecture-design |
| 2026-09-08 | Issue #236 / Issue #237 | Orchestrator Agent | Audited the framework for hook opportunities and context-budget duplication; filed two roadmap workstreams under #178 | Measured the canonical budget at 29,534/30,000 (headroom 466) and found zero pre-CI enforcement points despite 15 validators and 7 CI files — `.githooks/post-merge` had never run because `core.hooksPath` was unset. Identified four blocks of content written twice across two counted files with no sync validator. Filed Issue #236 (IMP-007) and Issue #237 (IMP-008), and flagged that #178's standing scope rule still names IMP-001 as the only authorised workstream, written before IMP-006 landed. | SA Agent | Next Action: SA design evaluation on both Issues before any implementation. Skill Used: dynamic-workflow (supporting: verification-before-completion) |
| 2026-09-07 | Issue #212 / PR #234 | Documentation Agent | Post-merge closeout for the release-skill New Feature; Issue #212 (IMP-006) closed in full | PR #234 squash-merged to `main` as `f5ff42c`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; final Completed entry added recording AC-07/AC-08 closure and all 8 ACs' PR history. The now-fully-resolved Issue #212 Blockers bullet removed entirely (no remaining open item for this Issue). Issue #212 closed on GitHub. | Human Maintainer | Next Action: none pending; repository is idle. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-07 | Issue #212 | QA Agent | Independent verification of Issue #212's AC-07 and AC-08 (final round) | Verified commit `fed6afb`. Independently confirmed byte-identity of the new skill across all three trees via direct sha256 comparison; performed a sentence-by-sentence distillation-accuracy check against `role-definitions.md`'s Release Agent section, confirming no invented policy (two non-blocking Observations noted: an unelaborated "change window" mention, and one historical-rationale parenthetical cut while the operative requirement was kept). Ran all 17 `validate:*`/`adr:audit`/`npm test` commands, several outside the original dispatch's Verify list, all PASS. Confirmed no human gate weakened. Flagged that the SA/Human decision record wasn't yet posted as an Issue comment — addressed before PR by posting it. | Documentation Agent | Next Action: post-merge closeout. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-07 | Issue #212 | Developer Agent | Implemented Issue #212's AC-07 — new release-readiness-checklist skill | Distilled Release Agent's existing `role-definitions.md` section (Versioning and Changelog Contract, Release Evidence Checklist, Triple Rollback Confirmation, Deployment Strategy Statement) into a new skill, byte-identical across all three trees, matching the Config/Documentation Agent precedent per SA's evaluation and Human Maintainer approval. New `SKILL_CATALOG.md` row; `## Skill Routing` added to `release-agent.md`; `ROLE_SOURCE_CONTRACT['Release Agent']` corrected from its `documentation-closeout` stand-in; `required-source-matrix.json` repinned. Context budget 29384→29534/30000. Ran in a sandboxed worktree isolated from the assigned branch; the parent session cherry-picked the commit onto `feat/issue-212-release-skill` and re-verified before dispatching QA. | QA Agent | Next Action: independent verification of AC-07/AC-08. Skill Used: tdd-implementation (supporting: mutation-testing, code-review-gate) |
| 2026-09-07 | Issue #212 (IMP-006) | Human Maintainer / SA Agent | Approved SA's AC-07 recommendation: create a new Release Agent skill, not an ADR-only route | SA evaluated whether Release Agent needs a dedicated skill or an ADR recording why not, found Config Agent and Documentation Agent both already embed policy content elsewhere and still own a skill, and recommended the same for Release Agent (~150-250 token cost against ~616 headroom). Human Maintainer approved creating the skill per SA's recommendation. Recorded on Issue #212 directly (not a separate tracking Issue, since AC-07's own text is satisfied by the artifact and needs no ADR under the "skill exists" branch). | Developer Agent | Next Action: dispatch Developer Agent to implement the approved skill. Skill Used: No matching skill — followed the dispatch packet's Method section directly. | PR #232 squash-merged to `main` as `555484e`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; #212 recorded under Completed with its QA evidence link. Blockers bullet updated to name only the two remaining ACs (AC-07 Release Agent skill decision, AC-08 final gate) instead of the now-stale AC-02–AC-08 list. | Human Maintainer | Next Action: decide AC-07 (new Release Agent skill vs. an ADR recording why none is needed), then dispatch Developer Agent if a new skill is chosen. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-07 | Issue #212 | QA Agent | Independent verification of Issue #212's AC-02 through AC-06 | Verified commit `1372e95`. Independently reproduced (not accepted from the Developer's claims): drift via an appended line, `MISSING` via a deleted adapter, and `NAME_DRIFT` via a `name:`-only change — the specific behavior ADR-0021 required. Context-budget before/after independently measured: 29985/30000 → 29384/30000, matching ADR-0021's estimate within 1 token. Spot-checked all 5 AC-06 adapters (not a sample) across all three trees. Confirmed two Developer-self-flagged items (`required-source-matrix.json`/`ROLE_SOURCE_CONTRACT` gap, `housekeeping:worktrees` failure) are genuinely pre-existing and unrelated to this diff, not accepted on claim. AC-06 correctly left open pending AC-07. Full suite 565/565, all gates PASS. | Documentation Agent | Next Action: post-merge closeout. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-07 | Issue #212 | Developer Agent | Implemented Issue #212's AC-02 through AC-06 | New `scripts/validate-adapter-parity.mjs` per ADR-0021's Option B: body-below-frontmatter comparison, `name:` pinned equal, fails closed on missing adapters. Seeded `.agents/agents/` and `.agent/agents/` (11 files each, did not exist before). Wired into both CI hosts. Relocated `role-definitions.md`'s Terminal Dispatch section into `task-execution-mode.md` and ran `repin:source-matrix`, recovering headroom to 29384/30000. Wired 5 unwired adapters to their existing skills via a new `## Skill Routing` section, mirrored across all three trees, following the Data Agent no-canonical-mirror precedent. AC-01/AC-07/AC-08 deliberately excluded from this dispatch's scope. Suite 551 to 565. Ran in a sandboxed worktree isolated from the assigned branch; the parent session cherry-picked both commits onto `feat/issue-212-adapter-parity` and re-verified before dispatching QA. | QA Agent | Next Action: independent verification of AC-02 through AC-06. Skill Used: tdd-implementation (supporting: mutation-testing, code-review-gate) |
| 2026-09-07 | Issue #229 / PR #230 | Documentation Agent | Post-merge closeout for the ADR-0021 Bug Fix | PR #230 squash-merged to `main` as `6a9d278`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; #229 recorded under Completed with its QA evidence link. The Issue #212 Blockers bullet trimmed to point at that Completed entry and restate only the still-unimplemented ACs (AC-02–AC-04, AC-06–AC-08), avoiding duplication with the ADR-0021 detail now in Completed. | Human Maintainer | Next Action: dispatch Developer Agent for Issue #212's remaining ACs, or hold for further sequencing. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-07 | Issue #212 / Issue #229 | Documentation Agent | Recorded Human Maintainer decision on Issue #212's AC-01/AC-05 as ADR-0021, no code change | Accepted Option B (body-below-frontmatter parity, `name:` pinned equal across trees) over A and C for AC-01. Approved relocating `role-definitions.md`'s Terminal Dispatch and Boss Visibility section into `task-execution-mode.md` for AC-05 (≈615 tokens headroom expected); declined the optional QA Cross-Platform Acceptance Criteria Gate relocation. Recorded in `DECISIONS.md` as ADR-0021 and `PROJECT_STATUS.md`'s Blockers. Implementation (parity script, the actual relocation edit, adapter wiring) not yet dispatched. | Developer Agent (once dispatched) | Next Action: dispatch Developer Agent for AC-02–AC-05 implementation, or hold for further Human Maintainer sequencing. Skill Used: documentation-closeout (supporting: git-workflow-and-versioning) |
| 2026-09-07 | Issue #226 / PR #227 | Documentation Agent | Post-merge closeout for the ADR-0020 Bug Fix | PR #227 squash-merged to `main` as `9889da6`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; #226 recorded under Completed with its QA evidence link. The prior "Resolved" Blockers bullet for the `githubJobCommands` design Question is folded into that Completed entry and removed; the still-deferred `normaliseCommand` finding remains a tracked Blockers item, now attributed to ADR-0020 and Issue #226. | Human Maintainer | Next Action: none pending; repository is idle. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-07 | Issue #210 / Issue #212 area | Documentation Agent | Recorded Human Maintainer decision on SA's `validate-ci-parity.mjs` evaluation as ADR-0020, no code change | Accepted SA's recommendation to keep `githubJobCommands`'s zero-command guard as-is — closes the Issue #210 round-3 design Question. Deferred the separately-surfaced `normaliseCommand` multi-line finding (confirmed latent, not currently live) rather than fixing it now. Both recorded in `DECISIONS.md` as ADR-0020 and folded into `PROJECT_STATUS.md`'s Blockers. | Human Maintainer | Next Action: none pending; revisit the deferred `normaliseCommand` finding only if a multi-line `run:` block is introduced. Skill Used: documentation-closeout (supporting: git-workflow-and-versioning) |
| 2026-09-07 | Issue #212 area / `validate-ci-parity.mjs` | SA Agent | Design evaluation of `githubJobCommands`'s zero-command guard, no code change | Recommended keeping the current pattern as-is: rereading the function shows it is one shape-independent postcondition, not a per-shape enumeration as the round-3 QA Question framed it, and round 3's own adversarial probe (four new shapes) found no gap. Rejected a positive-assertion allowlist (strictly less detection power, new staleness burden) and a snapshot/fixture pattern (this repo already has direct cost evidence against that shape from `repin-source-matrix.mjs`, Issue #215) for this specific gap. Separately surfaced a latent, not-currently-live risk one level down in `normaliseCommand`: a `run: \|` multi-line block's non-global `.match()` only extracts the first command, and a block whose first line matches `IGNORED_COMMAND_RE` is discarded whole. Not exercised today since every step in `.github/workflows/validate-contracts.yml` is single-line. Recorded in `PROJECT_STATUS.md` Blockers with the two decision questions the Human Maintainer needs to answer. | Human Maintainer | Next Action: accept/reject the recommendation and decide whether the `normaliseCommand` follow-up is worth fixing now. Skill Used: No matching skill — followed the dispatch packet's Method section directly. |
| 2026-09-07 | Issue #224 / PR #223 | Documentation Agent | Post-merge closeout for the decisions-recording Bug Fix | PR #223 squash-merged to `main` as `6fc6828`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; #224 recorded under Completed with its QA evidence link. The two prior Blockers bullets it resolved (RISKS.md restoration scope, PR #204's stale label) are folded into that Completed entry rather than left as separate "Resolved" bullets, since both are now fully closed with no further action possible. | Human Maintainer | Next Action: none pending; repository is idle. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-07 | PROJECT_STATUS.md Blockers | Documentation Agent | Recorded two Human Maintainer decisions, no code change | (1) `RISKS.md`'s 7 historical entries destroyed by earlier resets will not be reconstructed — no currently open Issue cites any by id, decided by the Human Maintainer directly rather than routed to any agent. (2) PR #204's stale `post-merge-closeout` label removed via `gh issue edit 204 --remove-label post-merge-closeout`, confirmed empty via the Issues API. Both Blockers items marked resolved in `PROJECT_STATUS.md`. | Human Maintainer | Next Action: none pending on this branch. Skill Used: documentation-closeout (supporting: git-workflow-and-versioning) |
| 2026-09-05 | Issue #220 / PR #221 | Documentation Agent | Post-merge closeout for the test-coverage-gaps Bug Fix | PR #221 squash-merged to `main` as `f1eb57d`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; #220 recorded under Completed with its QA evidence link and mutation-verified coverage claim. Two carried-forward Blockers items resolved and marked so: the `validate-risk-register.mjs` fail-open design Question (confirmed a deliberate `adr-audit.mjs`-consistent choice, with one dropped-conjunct mutation confirmed a genuine equivalent mutant, not a residual gap) and the two `repin-source-matrix.mjs` test-quality Minors (fixed). Nothing new opened by this closeout. | Human Maintainer | Next Action: none pending; repository is idle. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-05 | Issue #220 | QA Agent | Independent verification of both Issue #220 ACs | Verified commit `e257893`. AC-01 mutation-verified (undefined-fallback hardcode fails only the new test; `regressed = true` confirmed non-unique; dropping the `previousTotal !== undefined` conjunct confirmed a genuine equivalent mutant, `total < undefined` is always false in JS). AC-02 item 1 mutation-verified (neutered guard against the new stale-hash fixture fails on file-unmodified assertion; same mutation against the old correct-hash fixture fails only on "must throw," proving the old fixture was blind to this regression class). AC-02 item 2 confirmed by re-adding the deleted test and reproducing that it never fails alone. Full suite 551/551 (net zero), all gates PASS, no production code touched. | Documentation Agent | Next Action: post-merge closeout. Skill Used: No matching skill — followed the dispatch packet's Method and Verify sections directly. |
| 2026-09-05 | Issue #220 | Developer Agent | Closed both test-coverage gaps from Issue #220 | Added a test locking in `validate-risk-register.mjs`'s intentional fail-open behavior when `previousTotal === undefined`, mutation-verified against the undefined-fallback hardcode; checked two further candidate mutations from the dispatch packet and recorded, rather than forced, that neither gives unique evidence (one is a non-unique kill, one is a genuine equivalent mutant). Strengthened `repin-source-matrix.mjs`'s round-trip-guard test by pinning a deliberately stale fixture hash so the file-unmodified assertion can't be short-circuited by an already-correct hash. Removed the "stops after first occurrence" test after confirming it always failed in lockstep with the preceding test. No production code changed. Ran in a sandboxed worktree isolated from the assigned branch; the parent session cherry-picked both commits onto `fix/issue-220-test-coverage-gaps` and re-verified before dispatching QA. Suite 551/551 (net zero: +1, -1). | QA Agent | Next Action: independent verification of both ACs. Skill Used: tdd-implementation (supporting: mutation-testing, code-review-gate) |
| 2026-09-05 | Issue #215 / PR #217 | Documentation Agent | Post-merge closeout for the repin-source-matrix New Feature | PR #217 squash-merged to `main` as `e4bc02b`. `PROJECT_STATUS.md`'s Current Work Item reset to idle; #215 recorded under Completed with its QA evidence link and mutation-verified coverage claim. Applied `status:development-done` and `status:verification-done` to Issue #215 and moved it to `phase:human-review` before merge, since the `work-item-readiness-freshness` gate required them and both the Developer and QA reports had explicitly deferred applying them to the orchestrating session. Genuinely open items carried forward: two Minors on the new test suite's precision, routed to Developer Agent, not fixed here. | Human Maintainer | Next Action: none pending; repository is idle. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-05 | Issue #214 / PR #216 | Documentation Agent | Post-merge closeout for the risk-register Bug Fix | PR #216 squash-merged to `main` as `b44c057`. `PROJECT_STATUS.md`'s Current Work Item reset to idle on `main`, noting Issue #215 as the genuinely active item on its own branch; #214 recorded under Completed with its QA evidence link and mutation-verified coverage claim. Genuinely open items carried forward: the data-restoration scope question for `RISKS.md`'s 7 historical entries, and QA's fail-open Question on `validate-risk-register.mjs`'s unreadable-comparison-commit path. | Human Maintainer | Next Action: none pending on `main`; Issue #215 continues on its own branch. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-05 | Issue #208 / PR #209, Issue #210 / PR #211 | Documentation Agent | Post-merge closeout for both Bug Fix work items | PR #209 squash-merged to `main` as `e820389`; PR #211 squash-merged as `be15d7b` after being rebased onto the post-#209 `main` to resolve a merge-order dependency its own body had flagged (its `DECISIONS.md` had zero real ADRs until #209's restoration landed, which failed `adr-audit` in CI). `PROJECT_STATUS.md`'s Current Work Item reset to idle; both issues recorded under Completed with their QA evidence links. Genuinely open items carried forward rather than closed: the `required-source-matrix.json` re-pin tooling gap, the deferred `RISKS.md` decision, and Issue #210's round-3 design Question and cosmetic Minor. | Human Maintainer | Next Action: none pending; repository is idle. PR #204's stale `post-merge-closeout` label is a separate, already-parked item and was not touched by this closeout. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-05 | GitHub Issue #210 | Developer Agent | Closed a second and third fail-open regression found by independent re-review | Round 2 found CR-1114: scoping the GitHub-side comparison to the named `validate` job (to stop a false failure on a GitHub-only publish job) meant a renamed or restructured job resolved to an empty command set, and the check reported a silent `PASSED` while comparing nothing on either host. Fixed by throwing when the named job cannot be found. Round 3 found CR-1115: that guard only checked for a missing/malformed `steps` array, so a job restructured into a composite action or reduced to `steps: []` reached the identical silent-PASS outcome through a shape the guard did not cover. Fixed by also throwing when the extracted command set is empty, mutation-verified. `task_review_rework_count` reached its stated ceiling of 2 after round 2; round 3 proceeded only by explicit Human Maintainer decision to fix rather than accept as residual risk or stop for manual review. Suite 513 to 517. Independent QA then returned PASS with one non-blocking Minor (misleading error-message wording for one shape) and one design Question (three rounds targeting the same guard-scope defect class) flagged for the Human Maintainer, not fixed as a fourth round. | Human Maintainer | Next Action: merge decision for PR #211, with the Minor and Question carried forward as non-blocking notes. Skill Used: tdd-implementation (supporting: mutation-testing, code-review-gate) |
| 2026-09-05 | GitHub Issue #210 | Developer Agent | Closed the QA findings on the first #210 candidate | Critical was mine: a `node_modules` symlink containing an absolute home path was committed, because `.gitignore` used `node_modules/` and a trailing slash matches directories only, so a symlink walked past it; CI stayed green because `npm ci` replaces it, which is why nothing caught it. Removed from the index and the pattern corrected. The detector itself was defeatable three ways — a GitLab step commented out with `#` counted as coverage, `node scripts/x.mjs` and `npx x` were invisible, and reading the whole workflow file made a GitHub-only publish job a false failure — all fixed by parsing both files with the `yaml` package already in devDependencies and scoping to the named validate job. Exemptions now carry a required reason and are rejected when stale. One QA finding was checked and found factually wrong: `validate_project_state` is not a live asymmetry because GitHub also runs it only on push to main; the underlying principle that this check compares which commands run and never when they run is accepted and now printed on every run. CR-1104's claim that the three validators were verified token-independent is corrected: the API path is unreached, so exit 0 proved the path is dead, not portable. Suite 509 to 513. | QA Agent | Next Action: independent re-review scoped to the prior finding set and fix-caused regression; the dispatch-receipt token and owner/repo defaults are parked with an owner. Skill Used: tdd-implementation (supporting: code-review-gate, verification-before-completion) |
| 2026-09-05 | GitHub Issue #210 | Developer Agent | Closed the GitHub/GitLab validator gap and added a check so it cannot reopen silently | `validate:clearable-refs`, `validate:dispatch-receipts`, and `validate:workflow-evidence` ran on GitHub but not GitLab, so a GitLab-hosted clone was gated more weakly; all three are plain Node scripts with no host API dependency, verified to exit 0 with `GITHUB_TOKEN` unset before being added. Added `scripts/validate-ci-parity.mjs`, wired into both CI files, which fails when the GitHub validate job runs a named script GitLab does not; a deliberate asymmetry must be named in `HOST_ONLY_SCRIPTS` with a reason and a test asserts that list is empty. RED was taken by running the checker against the unmodified repository, where it exited 1 and named all three. A drafted `platform-readiness.md` edit was reverted because that file is pinned by sha256 in `required-source-matrix.json` and editing it broke 7 context-shadow tests; AC-05 is satisfied by the CI change itself. Suite 503 to 509. | QA Agent | Next Action: independently verify AC-01–AC-06 against the exact diff; the missing re-pin tooling for `required-source-matrix.json` is parked with an owner, not fixed here. Skill Used: tdd-implementation (supporting: code-review-gate, verification-before-completion) |
| 2026-09-05 | GitHub Issue #208 | Developer Agent | Closed a regression I introduced and a coverage claim that did not hold | Re-review disposed five of six prior findings as addressed and found two things caused by my own fix. First, the widened date pattern captured the closing `**` of an unfilled `**Date:**`, so a template stub counted as a recorded decision and the Issue #208 failure mode returned through a different door; the value must now start with a non-asterisk character, verified across eight field shapes. Second, and more serious: the coverage I claimed for the branch-walk fix was cosmetic. The reviewer deleted the entire walk and all 22 tests still passed, because the unconditional `HEAD~1` entry plus `Math.max` supplied the asserted count in every fixture. Added a three-commit fixture where the fork point and the parent both hold zero, so only walking the branch can answer, and validated it by mutation rather than assertion. That is the second time this record asserted coverage that did not exist. Documented the 100-commit walk bound and made an unreadable comparison print as absent rather than passing. Suite 514 to 518. | Human Maintainer | Next Action: `task_review_rework_count` is 2 of 2 — any further unresolved review result stops here rather than starting a third round. Skill Used: tdd-implementation (supporting: mutation-testing, code-review-gate) |
| 2026-09-05 | GitHub Issue #208 | Developer Agent | Closed the QA findings on the first #208 candidate | AC-05 failed: the regression guard compared only against the merge base, so ADRs added and then destroyed inside a feature branch were invisible — QA destroyed 5 and the audit passed. Their sharpest point was that a deliberately broken `GITHUB_BASE_REF` *did* catch it, since that fell through to `HEAD~1`; a guard that works better when misconfigured is inverted. The comparison now spans every commit in `mergeBase..HEAD` and takes the highest count held. A Major was also confirmed: a correctly-headed ADR written with `**Date:**` counted as zero, so the reset blanked it and the audit passed. Three Minors fixed, including that CR-1006 in the self-review was factually wrong; the wrong claim is left in the record rather than edited away. QA also proved neither earlier test touched the merge-base path, so the new fixtures build a real origin remote. Suite 509 to 514. | QA Agent | Next Action: independent re-review of the fix diff, scoped to the prior finding set and fix-caused regression. Skill Used: tdd-implementation (supporting: code-review-gate, verification-before-completion) |
| 2026-09-05 | GitHub Issue #208 | Developer Agent | Stopped the reset from destroying the decision log, and made the audit able to see it | Found while investigating PR #204's stale closeout label: `scripts/reset-to-template.mjs` stubbed `DECISIONS.md` like a record, taking the ADR count 17 to 0 at `93203e2` and 2 to 0 at `aa2a871`. Open Issues #132/#133 cite ADR-0017 and #203 depends on ADR-0019, all destroyed. `adr-audit` could not detect it because a reset blanks `TASK_LOG.md` in the same commit, making the ratio 0/0 and reporting PASS. The apply path now refuses when real ADRs exist unless `--reset-decisions` is passed, listing the ids; the audit fails closed when the count drops against the merge base with the declared base branch, and an unreadable comparison ref returns undefined rather than zero so it cannot fail open. ADR-0017 and ADR-0019 restored; the remaining 16 were excluded by Human Maintainer decision and their recovery command is recorded in the file. `RISKS.md` is the same class and was deliberately deferred to its own decision. TDD with four failing tests first; suite 503 to 509. | QA Agent | Next Action: independently verify AC-01–AC-06 against the exact diff. Skill Used: tdd-implementation (supporting: code-review-gate, debugging-discipline) |
| 2026-09-05 | Framework assessment | Orchestrator Agent | Re-derived the improvement backlog from measured repository evidence | Recorded at `docs/records/misc/2026-09-05-framework-sdlc-assessment.md`. Canonical context budget measured at 29,985/30,000, leaving 15 tokens, which blocks any new role or skill text; Issue #178's own scope rules forbid raising the target, so the conflict is the Human Maintainer's to resolve. Role adapters exist only under `.claude/agents/` and `validate-skill-parity` covers only `*/skills`, so the layer with the least protection is the one the request most wants to strengthen; this is roadmap IMP-006, which was never opened as an issue. Six of eleven roles route to no skill and Release Agent owns none at all. GitLab splits into three portable validators missing from `.gitlab-ci.yml` and five API gates that need credentials and a security review. Closed #206; pruned 2 of 11 worktrees after verifying the other 9 hold dirty files or unmerged commits. | Human Maintainer | Next Action: decide the context-budget lever before any role/skill expansion. Skill Used: dynamic-workflow (supporting: verification-before-completion) |
| 2026-08-22 | Maintenance Issue #206 / PR #205 | Documentation Agent | Completed post-merge closeout for blank-template reset | PR #205 merged as `aa2a871`; 39 historical entries were reset in the isolated candidate, canonical workflow/skill files were unchanged, and QA passed 503/503. | Human Maintainer | Next Action: none; repository is at the blank-template baseline. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
| 2026-09-05 | GitHub Issue #215 | Developer Agent | Added a re-pin script for `required-source-matrix.json`'s sha256 hashes | New `scripts/repin-source-matrix.mjs` (`npm run repin:source-matrix`) walks `matrix.rows[].requiredSources[]` directly, groups every occurrence of each unique path, fails closed (writes nothing, non-zero exit) if a path's existing pinned hashes disagree with each other or if the fixture no longer round-trips byte-identically through `JSON.stringify(JSON.parse(raw), null, 2) + '\n'`, updates every occurrence of a path whose content changed (not just the first match), and performs no filesystem write at all when nothing changed. Exported the existing `sha256(bytes)` helper from `scripts/lib/context-compatibility-v1.mjs` (visibility-only change) for hash-computation parity with the validator. TDD: 9 tests written first, all failing (`ERR_MODULE_NOT_FOUND`) before implementation; mutation-verified the every-occurrence claim, the fail-closed guard, and the round-trip invariant guard each independently kill their own test when removed. Reproduced the Issue's own documented scenario against real files (`docs/workflow/platform-readiness.md`, then `AGENTS.md` pinned 22×), confirmed the exact same 7 named test failures, ran the script, confirmed 541/541 green, then reverted the experiment. Documented the command in `docs/operating-model/CONTEXT_BUDGET.md` rather than `docs/workflow/platform-readiness.md`, since the latter is itself one of the 25 sha256-pinned paths. Suite 532 → 541. Independent QA then passed at `ea6767b` (all three mutation claims independently re-verified) with two non-blocking Minors on test-quality precision. | Human Maintainer | Next Action: merge decision for PR #217. Skill Used: tdd-implementation (supporting: mutation-testing, verification-before-completion) |
| 2026-09-05 | GitHub Issue #214 | Developer Agent | Stopped the reset from destroying the risk register, and made the audit able to see it | Same class of defect as #208, this time in `RISKS.md`: `scripts/reset-to-template.mjs` stubbed it like a record, taking the count 7 to 0 (PR #162) and 4 to 0 (PR #205), per the Issue's own evidence table. `validate-risk-register` could not detect it because `countOpenRisks` only ever read the current file, with no baseline to regress against. The apply path now refuses when real risk entries exist unless `--reset-risks` is passed, listing the ids; the validator fails closed when the total (not open) count drops against every reachable comparison commit — merge base, every commit walked to HEAD capped at 100, and HEAD~1 — reusing `comparisonRefs` imported directly from `adr-audit.mjs` rather than duplicating it. Total, not open, is the regression signal: closing a risk in place is legitimate and must not trip the guard. AC-01 investigation (grep across the repo plus every open Issue body) found no currently open Issue cites a specific `RISKS.md` id, confirming the Orchestrator's routing-comment finding independently; per explicit instruction, no historical risk content was restored — that data-restoration scope question is left for the Human Maintainer. TDD with ten failing tests first across both files; each of the comparison-walk logic, the regression check, and the reset guard was individually confirmed load-bearing by deleting it and watching the corresponding test fail, then restoring it. Suite 532 to 542. | QA Agent | Next Action: independently verify AC-02–AC-06 against the exact diff; AC-01 is closed by investigation, not restoration, and that scope question is parked for the Human Maintainer. Skill Used: tdd-implementation (supporting: mutation-testing, code-review-gate) |
