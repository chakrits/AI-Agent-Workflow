# TASK_LOG.md

| Date | Work Item | Agent | Action | Result | Next Agent | Notes |
|---|---|---|---|---|---|---|
| 2026-09-05 | GitHub Issue #208 | Developer Agent | Closed the QA findings on the first #208 candidate | AC-05 failed: the regression guard compared only against the merge base, so ADRs added and then destroyed inside a feature branch were invisible — QA destroyed 5 and the audit passed. Their sharpest point was that a deliberately broken `GITHUB_BASE_REF` *did* catch it, since that fell through to `HEAD~1`; a guard that works better when misconfigured is inverted. The comparison now spans every commit in `mergeBase..HEAD` and takes the highest count held. A Major was also confirmed: a correctly-headed ADR written with `**Date:**` counted as zero, so the reset blanked it and the audit passed. Three Minors fixed, including that CR-1006 in the self-review was factually wrong; the wrong claim is left in the record rather than edited away. QA also proved neither earlier test touched the merge-base path, so the new fixtures build a real origin remote. Suite 509 to 514. | QA Agent | Next Action: independent re-review of the fix diff, scoped to the prior finding set and fix-caused regression. Skill Used: tdd-implementation (supporting: code-review-gate, verification-before-completion) |
| 2026-09-05 | GitHub Issue #208 | Developer Agent | Stopped the reset from destroying the decision log, and made the audit able to see it | Found while investigating PR #204's stale closeout label: `scripts/reset-to-template.mjs` stubbed `DECISIONS.md` like a record, taking the ADR count 17 to 0 at `93203e2` and 2 to 0 at `aa2a871`. Open Issues #132/#133 cite ADR-0017 and #203 depends on ADR-0019, all destroyed. `adr-audit` could not detect it because a reset blanks `TASK_LOG.md` in the same commit, making the ratio 0/0 and reporting PASS. The apply path now refuses when real ADRs exist unless `--reset-decisions` is passed, listing the ids; the audit fails closed when the count drops against the merge base with the declared base branch, and an unreadable comparison ref returns undefined rather than zero so it cannot fail open. ADR-0017 and ADR-0019 restored; the remaining 16 were excluded by Human Maintainer decision and their recovery command is recorded in the file. `RISKS.md` is the same class and was deliberately deferred to its own decision. TDD with four failing tests first; suite 503 to 509. | QA Agent | Next Action: independently verify AC-01–AC-06 against the exact diff. Skill Used: tdd-implementation (supporting: code-review-gate, debugging-discipline) |
| 2026-09-05 | Framework assessment | Orchestrator Agent | Re-derived the improvement backlog from measured repository evidence | Recorded at `docs/records/misc/2026-09-05-framework-sdlc-assessment.md`. Canonical context budget measured at 29,985/30,000, leaving 15 tokens, which blocks any new role or skill text; Issue #178's own scope rules forbid raising the target, so the conflict is the Human Maintainer's to resolve. Role adapters exist only under `.claude/agents/` and `validate-skill-parity` covers only `*/skills`, so the layer with the least protection is the one the request most wants to strengthen; this is roadmap IMP-006, which was never opened as an issue. Six of eleven roles route to no skill and Release Agent owns none at all. GitLab splits into three portable validators missing from `.gitlab-ci.yml` and five API gates that need credentials and a security review. Closed #206; pruned 2 of 11 worktrees after verifying the other 9 hold dirty files or unmerged commits. | Human Maintainer | Next Action: decide the context-budget lever before any role/skill expansion. Skill Used: dynamic-workflow (supporting: verification-before-completion) |
| 2026-08-22 | Maintenance Issue #206 / PR #205 | Documentation Agent | Completed post-merge closeout for blank-template reset | PR #205 merged as `aa2a871`; 39 historical entries were reset in the isolated candidate, canonical workflow/skill files were unchanged, and QA passed 503/503. | Human Maintainer | Next Action: none; repository is at the blank-template baseline. Skill Used: documentation-closeout (supporting: verification-before-completion, git-workflow-and-versioning) |
