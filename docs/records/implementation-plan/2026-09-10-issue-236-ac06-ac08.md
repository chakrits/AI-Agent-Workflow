# Implementation Plan: Issue #236 AC-06–AC-08

## Source and decision

- Issue: https://github.com/chakrits/AI-Agent-Workflow/issues/236
- Design authority: `DECISIONS.md` ADR-0022
- Candidate base: `main` @ `59c75f6`
- Change type: Framework / Meta Change
- Risk: Medium; local advisory hooks run after edits and must not become commit gates

## Objective

Add the remaining file based PostToolUse feedback for source matrix repinning,
context budget validation, and adapter/skill parity while keeping all rule logic
in portable `scripts/*.mjs` and `.claude/settings.json` as a thin invoker.

## Scope

- AC-06: derive the repin target set from `required-source-matrix.json` and run
  `npm run repin:source-matrix` after an Edit/Write match.
- AC-07: import `CANONICAL_FILES` from `validate-context-budget.mjs` and run
  `npm run validate:context-budget` after a match.
- AC-08: match `.claude/agents/**` and `.claude/skills/**` and run both parity
  validators.
- Tests, implementation record, and required project state updates.
- Out: AC-09, AC-11, AC-12, AC-10, Issue #237, commit hooks, lifecycle changes.

## Task breakdown and dependency order

1. Add focused tests for payload parsing, path classification, command selection,
   advisory execution, and Claude PostToolUse wiring. Run the focused tests to
   record RED before implementation.
2. Add the portable edit guard dispatcher and npm entry point. Keep source path
   discovery in the matrix and canonical export; do not duplicate those sets in
   the hook configuration.
3. Wire `.claude/settings.json` to the dispatcher as a PostToolUse thin invoker,
   then run focused tests and the relevant validators.
4. Add the code review record, update `TASK_LOG.md` and `PROJECT_STATUS.md`, and
   run the complete verification matrix before handoff.

## Verification

- Focused edit guard tests, including RED before implementation.
- `npm test`
- `npm run validate:ci-parity`
- `npm run validate:context-budget`
- `npm run validate:skill-parity`
- `npm run validate:adapter-parity`
- `npm run validate:contracts`
- `npm run validate:project-state`
- `git diff --check`
- Confirm `core.hooksPath` was not changed and no pre-commit hook was added.

## Risks and rollback

- A malformed hook payload must be ignored safely because the edit has already
  landed; the dispatcher must not become a blocking gate.
- A path can match more than one guard, so all applicable validators must run.
- Rollback is removing the dispatcher, its npm entry, settings wiring, tests,
  and records; existing validators remain unchanged.

## Handoff

Developer Agent → independent Code Review → independent QA → Human Maintainer.
The Developer report will include the candidate SHA, changed files, direct AC
evidence, context budget output, hook activation state, and known limitations.
