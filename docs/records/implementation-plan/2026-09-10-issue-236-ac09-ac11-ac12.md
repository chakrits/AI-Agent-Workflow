# Implementation Plan: Issue #236 AC-09, AC-11, AC-12

## Source and decision

- Issue: https://github.com/chakrits/AI-Agent-Workflow/issues/236
- Design authority: `DECISIONS.md` ADR-0022
- Change type: Framework / Meta Change
- Risk: Medium; SessionStart and SubagentStop hooks are display/advisory only

## Objective

Expose current work context at SessionStart, run the two existing dispatch/skill
validators advisorially at SubagentStop, and test that Claude rules are contained
by `.githooks/` or CI while surfacing the test-only QA evidence validator.

## Scope

- AC-09: display Current Work Item, Current Stage, branch, and worktree.
- AC-11: invoke `validate:dispatch-receipts` and `validate:skill-usage` at
  SubagentStop without blocking.
- AC-12: test hook/CI containment and explicitly classify
  `validate-qa-evidence.mjs` as test-only because it has no runtime entry point.
- Tests, review record, TASK_LOG, and PROJECT_STATUS updates.
- Out: AC-10, changes to AC-02–AC-08 behavior, Issue #237, commit blocking,
  lifecycle contracts, and validator rewrites.

## Verification

- Focused SessionStart, SubagentStop, and containment tests.
- `npm test`, all contract/parity/project-state/review/context-budget gates,
  and `git diff --check`.
- Mutation probes for display fields, advisory continuation, and containment.

## Handoff

Developer Agent → independent QA → Human Maintainer review. No PR or merge is
performed by the Developer Agent.
