# Implementation Plan — Issue #237 AC-13

## Objective

Move the backward-routing and QA-skip policy to `docs/workflow/dynamic-routing.md` as the canonical owner, then leave boot-discoverable imperative pointers in `AGENTS.md`.

## Scope

- Add the six role-to-role backward-routing rules and five skip rules to one canonical section.
- Replace the duplicated `AGENTS.md` sections with pointers to that section.
- Add a contract test that checks every policy sentence, both pointers, and absence of the stale source block.
- Re-pin the source matrix and record AC-11's verified `26,065 / 30,000` budget evidence.

## Out of scope

AC-14 consumer updates, Issue #244, unrelated routing refactors, and any change to the policy meaning.

## Verification

Run the focused AC-13 test, `npm test`, all context/contract/project-state/review/CI/parity validators, repin idempotence, `git diff --check`, and sentence-level plus mutation probes for each pointer, destination sentence, and stale-source regression.

## Risks and rollback

The risk is accidental policy loss or a second owner. The contract test and sentence comparison fail closed on either condition. Rollback is reverting this candidate commit.

## Handoff

Developer -> independent QA after candidate commit. Human approval remains required before PR merge.
