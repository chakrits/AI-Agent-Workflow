# Implementation Plan — Issue #237 AC-14

## Objective

Review the five named documentation consumers of `SKILL_CATALOG.md` after AC-08/AC-09 changed the catalog from per-skill headings to five-column tables. Update only consumers whose meaning or recorded measurements are stale, and record compatible consumers without unnecessary edits.

## Scope

- In: `CONTEXT_BUDGET.md`, the operating-model README, the vault index, `stabilize-core.md`, and `AGENT_EVALUATION_CHECKLIST.md`; a focused consumer contract test; implementation and state records.
- Out: catalog rows, AC-11 threshold, AC-13 policy, unrelated documentation cleanup, and Issue #244.

## Inspection decisions

| Consumer | Dependency inspected | Decision |
|---|---|---|
| `docs/operating-model/CONTEXT_BUDGET.md` | Recorded catalog size and current budget evidence | Update measured catalog totals and baseline metadata; the old per-file values were stale after the catalog collapse. |
| `docs/operating-model/README.md` | Read-order link to `SKILL_CATALOG.md` | Leave unchanged; it names the file and does not depend on per-skill headings or columns. |
| `docs/vault/00-Index.md` | Catalog link and hand-maintained skill inventory | Leave unchanged; its inventory links platform skill files and does not parse catalog headings or table columns. |
| `docs/workflows/stabilize-core.md` | Required-artifact link to the catalog | Leave unchanged; the workflow requires the catalog file, not its former internal shape. |
| `docs/operating-model/AGENT_EVALUATION_CHECKLIST.md` | Skill-selection checklist reference | Leave unchanged; the checklist points agents to the catalog and does not select by heading or column. |

## Verification

- Add a focused test proving all five consumer references remain valid and no named consumer contains removed per-skill heading patterns.
- Run the focused test, `npm test`, all required validation commands, source-matrix repin/idempotence, and `git diff --check`.
- Apply deletion/retarget mutations to the changed budget consumer and focused stale-shape guard; kill each non-equivalent mutant or classify equivalent mutants.

## Risks and rollback

This is documentation and contract-test maintenance. The main risk is recording a misleading budget baseline; rollback is an atomic revert of the consumer/test/record changes. No runtime, hook, catalog-row, or routing-policy behavior changes.

## Handoff

Independent QA must re-derive each consumer decision from current main and verify the focused guard, budget evidence, exact-base diff, and mutation results before PR creation.
