# Code Review Findings

Scope: stops `scripts/reset-to-template.mjs` from silently blanking a `RISKS.md` that holds recorded risk entries, and makes `scripts/validate-risk-register.mjs` fail when the risk register shrinks relative to a prior commit. Work item: [Issue #214](https://github.com/chakrits/AI-Agent-Workflow/issues/214). Governing workflow: Bug Fix (`docs/contracts/bug-fix-workflow.yaml`), same class of defect as Issue #208 (`DECISIONS.md`, PR #209, `e820389`).

| Finding ID | Severity | File / Area | Finding | Recommendation | Blocks Progress? | Evidence |
|---|---|---|---|---|---|---|
| CR-2001 | Critical | `scripts/reset-to-template.mjs` | `RISKS.md` is listed in `STUB_CONTENT` and blanked unconditionally on `--apply --confirm-reset`, with no equivalent to the ADR guard added in #208. Measured risk-entry count went 7 → 0 at `93203e2` (PR #162) and 4 → 0 at `aa2a871` (PR #205), per Issue #214's own evidence table | Refuse the apply path when real risk entries are present, naming the ids, unless `--reset-risks` is passed — mirroring `--reset-decisions` | No — fixed in this diff | `git show afe8091:RISKS.md` (7 entries) vs `main` (0 rows below the header) |
| CR-2002 | Critical | `scripts/validate-risk-register.mjs` | `countOpenRisks` only ever read the current file's row count. A reset that blanks `RISKS.md` to 0 rows reports `Total: 0, Open: 0` and the validator passes, because it had no baseline to regress against | Compare the current total against every reachable comparison commit (merge base with base branch, every commit walked from there to HEAD capped at 100, and `HEAD~1`) and fail closed on any decrease, mirroring `adr-audit.mjs`'s `comparisonRefs` | No — fixed in this diff | Test `runRiskValidation fails when the total risk count drops relative to the previous commit`; CLI output `Risk register validation FAILED: RISKS.md shrank from 2 to 0 entries.`, exit 1 |
| CR-2003 | Major | `scripts/validate-risk-register.mjs` — metric choice | Comparing the **open** count (rather than total) would fire on a legitimate lifecycle event: marking a risk `Closed` reduces `open` without destroying anything | The regression check compares `total`, not `open`. The pre-existing `activeWork && open === 0` check is left untouched and still governs the open-risk warning independently | No | Test `runRiskValidation does not fail when open risks drop but total is unchanged (legitimate closure)` |
| CR-2004 | Major | Single-sourcing / cross-script reuse | Re-implementing the merge-base + branch-walk comparison logic inside `validate-risk-register.mjs` would let it drift from `adr-audit.mjs`'s definition, and duplicating it would require a second mutation test for the same logic | `comparisonRefs` is imported directly from `scripts/adr-audit.mjs` (already exported, generic, no `DECISIONS.md` coupling) rather than duplicated. `countRisksInContent` was extracted so file-reading (`countOpenRisks`) and ref-reading (`countRisksAtRef`) share one row-parsing definition, following the `countAdrsInContent` precedent from #208 | No | `scripts/validate-risk-register.mjs` imports `{ comparisonRefs }` from `./adr-audit.mjs`; `adr-audit.mjs` itself is unmodified (out of scope per Issue #214) |
| CR-2005 | Minor | Refusal ordering | A refusal that ran after directory clearing, or after the DECISIONS.md guard's own mutation path, would still risk destroying records | The RISKS.md check is placed immediately after the DECISIONS.md check and before `dirtyTargets`/`applyReset`, so nothing mutates before either guard can fire | No | Test asserts `snapshotDeclaredTargets` is byte-identical after the RISKS.md refusal |
| CR-2006 | Minor | Test fixture defect (pre-existing, found here) | `makeFixtureRepo()` in `test/reset-to-template.test.mjs` seeded `RISKS.md` with a real `\| R-001 \|` row. Adding the new guard would have made every CLI test built on this fixture fail on the new refusal, masking the actual behavior under test | Rewrote the shared fixture's `RISKS.md` to hold no `\| R-\` rows (matching the fixture's existing pattern for `DECISIONS.md`, which already held only stub-shaped ADR content before #208's fixture-with-real-ADR helper was added), and added a dedicated `initGitFixtureWithRealRisk()` helper for the guard-specific tests, mirroring `initGitFixtureWithRealAdr()` | No | All 24 `reset-to-template.test.mjs` tests pass; the fixture's `RISKS.md` content still differs from `STUB_CONTENT['RISKS.md']` byte-for-byte so `planReset`'s "changed" assertion is unaffected |
| CR-2007 | Question (AC-01) | Data restoration scope | Should any of the historically destroyed risk entries be reconstructed even though no open Issue currently cites a specific `R-` id? | Not decided here. Per the Orchestrator's routing comment and this packet's explicit instruction, AC-01 is satisfied by investigation and report, not by guessed restoration. Grepped every `*.md`/`*.mjs`/`*.yaml` in the repository (excluding `.worktrees`) for `R-[0-9]` and every currently open Issue body (#132, #133, #136, #178, #192, #193, #203, #212, #215) for a citation of a specific `RISKS.md` id: none found. The only hits were test fixtures, template placeholders, historical (July 2026) QA-record prose referencing a now-destroyed `R-002`, and an unrelated `R-001` naming convention in `docs/workflow/role-definitions.md` that refers to a standing CI rule, not a `RISKS.md` row. `RISKS.md` is left unmodified by this diff — not an omission, the deliberate outcome of AC-01's negative finding | No — parked for Human Maintainer per the routing comment | This record's Verification section, "AC-01 investigation" |

## Verification

### TDD

- Wrote 10 new failing tests first, across both files, before touching implementation:
  - `test/validate-risk-register.test.mjs`: 6 new regression-detection tests (`runRiskValidation fails when the total risk count drops...`, `...does not fail when unchanged or grows`, `...does not fail when open risks drop but total is unchanged`, CLI exit-1 test, and two branch-walk tests) — RED at 13/19 passing before implementation (6 failures, all in the new tests; the pre-existing 13 untouched).
  - `test/reset-to-template.test.mjs`: 3 new guard tests (`CLI refuses to blank RISKS.md...`, `CLI blanks RISKS.md only when --reset-risks...`, `a RISKS.md with no real entries still resets...`) — RED at 23/24 (the refusal test failed with "Missing expected rejection", the other two incidentally passed because no guard yet existed to block them, which is expected for a "still works" and an "opt-in flag has no effect yet" case).
- Implemented `countRisksInContent`/`countRisksAtRef`/regression logic in `validate-risk-register.mjs`, then the `--reset-risks` guard in `reset-to-template.mjs`. Both files GREEN afterward: 19/19 and 24/24.

### Mutation verification (not just assertion-reading)

- Deleted the `rev-list` walk block inside the shared `comparisonRefs` (in `adr-audit.mjs`, restored immediately after): `a loss in the middle of a branch is found only by walking the branch` failed (`0 !== 5`), 18/19. Confirms the walk is load-bearing for the risk-register test, not incidentally passing via the `HEAD~1` fallback the way CR-1014 found for the original ADR-audit record.
- Set `const regressed = false; // MUTATED` in `validate-risk-register.mjs`, restored immediately after: 4 tests failed (15/19). Confirms the regression check itself is load-bearing.
- Deleted the `--reset-risks` guard block inside `reset-to-template.mjs`'s `main()`, restored immediately after: exactly 1 test failed (`CLI refuses to blank RISKS.md...`, 23/24). Confirms the guard is what the refusal test is actually exercising.
- `adr-audit.mjs` is unmodified in the final diff (`git diff` shows it only appearing transiently during the mutation probes above, restored via `cp` from a pre-edit backup) — confirmed out of scope per Issue #214's Scope section.

### AC-01 investigation

- Ran `grep -rnE '\bR-[0-9]' --include='*.md' --include='*.mjs' --include='*.yaml' .` (excluding `.worktrees` and `node_modules`) and checked every currently open Issue's body (#132, #133, #136, #178, #192, #193, #203, #212, #215) for an `R-\d+` token. No open Issue cites a specific `RISKS.md` entry by id. The only historical hit citing a risk id (`R-002`, in two July 2026 `docs/records/qa/*.md` files) predates both resets and is not referenced by any currently open Issue. Confirms the Orchestrator's routing-comment finding independently rather than trusting it uncorroborated.
- Per this packet's instruction, no historical risk content is restored on this initiative. `RISKS.md` is untouched by this diff.

### Full-suite and validator results

- `npm test`: 542 / 542 passing (532 before this branch's tests were added; +10 net across both test files — 6 net-new assertions plus 4 that already existed as unaffected pre-existing tests re-verified).
- `validate:contracts`, `validate:project-state`, `validate:skill-parity`, `validate:risk-register`, `adr:audit`, `validate:skill-usage`, `validate:metrics`, `validate:context-budget`, `validate:review-gate`, `validate:ci-parity`: all PASS.
- `git diff --check`: clean, no whitespace errors.
- `npm run validate:risk-register` on this repository (which still holds 0 risk entries, unaffected by this diff) reports `Total risk entries: 0 … Previous total: 0 … PASSED` — the regression guard correctly reports no regression, since `RISKS.md` was already at 0 before and after this branch, and `validate:metrics` independently confirms `Risks tracked: 0 (0 open, 0 closed)`.

## Review Decision

Approved (self-review). CR-2001 and CR-2002 are the defect and the reason nothing caught it — the direct risk-register analogue of CR-1001/CR-1002 in the #208 record. CR-2003 documents the one place this fix deliberately diverges from a naive mirror of #208 (total vs. open) and why. CR-2004 keeps the two scripts' comparison logic from drifting apart. CR-2005 protects the records a refusal is supposed to save. CR-2006 is a fixture defect this work surfaced and fixed before it could mask the new guard's own tests. CR-2007 records the AC-01 investigation and its negative result, per the Fallback and AC-01 instructions in this dispatch — not an oversight.

## Independent Review

Not dispatched at self-review time. Recorded per this repository's `code-review-gate` convention before requesting independent QA verification of Issue #214's Acceptance Criteria.
