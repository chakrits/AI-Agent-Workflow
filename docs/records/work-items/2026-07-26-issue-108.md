# Work Item: Issue #108 (backfilled)

## Source
- Issue: https://github.com/chakrits/AI-Agent-Workflow/issues/108
- Umbrella issue: N/A
- Boss directive: N/A — generated by `scripts/backfill-work-item-records.mjs` from historical TASK_LOG.md rows

## Classification
- Change type: Unknown — requires review
- Risk level: Unknown — requires review
- Workflow route: Unknown — requires review

## Artifacts
- Requirement: N/A
- SDD: N/A
- Implementation plan: N/A
- PRs: Unknown — requires review
- Closeout PR: Unknown — requires review
- Postmortem: N/A

## Sub-tasks
- 2026-07-26 — Documentation Agent (session): Post-merge documentation closeout for the lessons-learned record and skill patch
- 2026-07-26 — Documentation Agent (session): Combined post-merge documentation closeout for both related Bug Fix work items
- 2026-07-26 — QA Agent (second re-verification): Independently re-verified the two-signal fix and found it reintroduced the same defect class one level down: `.github/pull_request_template.md`'s own instructional guidance text contains the literal matchable phrase `Governing workflow: Bug Fix`, and the unanchored regex could not distinguish that boilerplate prose from a genuine authored declaration — an unedited template plus a `bug` label would pass without the author ever declaring anything. Returned BLOCKED, named Human Maintainer as owner, offered two remedies without choosing one.
- 2026-07-26 — QA Agent → Human Maintainer → Developer Agent (session): Independent QA verification (PASS on all 5 ACs) flagged that the label-only carve-out let any Issue mislabeled `bug` silently bypass the entire lifecycle gate on a required merge check, and that the "Feature/Enhancement unaffected" test was byte-for-byte identical to a pre-existing test with zero real AC-02 coverage. Human Maintainer chose Candidate Approach 2 (a PR-body-declared governing workflow) to combine with the label rather than the label alone
- 2026-07-26 — Developer Agent (session): Fixed `work-item-readiness.mjs`'s missing Bug Fix workflow carve-out using TDD, discovered when PR #107 (Issue #106's own fix) failed `work-item-readiness-freshness` because the script unconditionally required `status:spec-ready`/`status:development-done`/`status:verification-done`, labels that `AGENTS.md` documents Bug Fix work items correctly never carry

## Provenance
- TASK_LOG.md row: `2026-07-26 | Issue #106/#108 retrospective / PR #112` — PR #112 merged as `36031fa`; `PROJECT_STATUS.md`, `TASK_LOG.md`, and `CHANGELOG.md` synchronized; project remains idle (no Current Work Item reset needed — it was already idle). The automated "Create documentation sync exception" workflow applied the `post-merge-closeout` label to PR #112 automatically.
- TASK_LOG.md row: `2026-07-26 | GitHub Issue #106 / PR #107 + GitHub Issue #108 / PR #109` — PR #109 merged as `9408abe`; PR #107 merged as `812e7df`. `PROJECT_STATUS.md`, `TASK_LOG.md`, and `CHANGELOG.md` synchronized for both; project reset to idle. Both PRs are governed by the Bug Fix contract, not the `phase:`/`status:` lifecycle label contract, and neither carries lifecycle labels to remove.
- TASK_LOG.md row: `2026-07-26 | GitHub Issue #108` — Fixed by anchoring the regex to line start: `/^Governing workflow:\s*Bug Fix\b/im`. Reproduced the defect empirically before fixing (unedited template matched: true) and confirmed after (false), with the legitimate multi-line declaration shape still matching. 3 new regression cases added, one using the real `.github/pull_request_template.md` file itself as an adversarial fixture (same pattern as Issue #106's fix, which used the real `docs/records/qa/` directory rather than synthetic data). `npm test` 214 → 217; `validate:contracts`, `validate:skill-parity`, `validate:skill-usage` all pass.
- TASK_LOG.md row: `2026-07-26 | GitHub Issue #108` — RED→GREEN via TDD: `validateReadiness()`'s Bug Fix branch now requires **both** `labels.includes('bug')` **and** a `Governing workflow: Bug Fix` line in the PR body; absent the declaration, a `bug`-labeled item falls through to the strict path. 2 new adversarial regression cases added (label without declaration; declaration without label) plus the redundant test replaced with genuine non-draft coverage — 5 → 7 Bug Fix cases, 212 → 214 total. `.github/pull_request_template.md` documents the new field so it isn't tribal knowledge. Verified against the real Issue #106/PR #107 shape both ways (declaration present → `[]`; absent → falls through and fails). `npm test`, `validate:contracts`, `validate:skill-parity`, `validate:skill-usage` all pass.
- TASK_LOG.md row: `2026-07-26 | GitHub Issue #108` — RED: 4 of 5 new regression cases failed against the unmodified script. GREEN: `validateReadiness()` gains an early `labels.includes('bug')` branch that checks only QA evidence (when not draft) and bypasses the phase-count and `status:*` checks entirely, since `AGENTS.md`'s lifecycle label contract section scopes both label categories to Feature/Enhancement work. Verified directly against Issue #106's real label/body shape (`['bug','phase:requirements']` + valid QA evidence line) returns `[]`. `npm test` 207 → 212; `validate:contracts`, `validate:skill-parity`, `validate:skill-usage` all pass. Review record: `docs/records/qa/2026-07-26-issue-108-readiness-bugfix-carveout-code-review.md`.

## Lessons Learned
- N/A

## Metrics
- Tests before: N/A
- Subagent timeouts: N/A
- Rework cycles: N/A
- Packet version: N/A

## Status: Closed (2026-07-26)

<!-- Generated by `scripts/backfill-work-item-records.mjs`. Do not hand-edit generated status without also correcting the underlying TASK_LOG.md evidence. -->
