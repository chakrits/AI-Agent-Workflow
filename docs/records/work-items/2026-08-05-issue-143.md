# Work Item: Issue #143 — Decision Table + State Transition testing techniques for functional-test-design

## Source
- Issue: https://github.com/chakrits/AI-Agent-Workflow/issues/143
- Umbrella issue: N/A
- Boss directive: 2026-08-05 — screen an external `skill.md` "Software Testing & Quality Assurance Agent Skill Guide" example against this repo's existing `functional-test-design`/`test-quality-discipline`/`qa-playwright-testing` skills, adopt only genuine gaps, argue against the rest without flattery

## Classification
- Change type: Framework / Meta Change (skill/catalog authoring, no target-app code)
- Risk level: Low — documentation/instruction content only, no CI/build/runtime behavior change
- Workflow route: Documentation Agent (authoring) → self-review (code-review-gate) → independent review → Human Maintainer (merge)

## Artifacts
- Requirement: N/A — scoped directly through conversation (screening table in the implementation plan serves as the requirement record)
- SDD: N/A
- Implementation plan: [[../implementation-plan/2026-08-05-functional-test-design-technique-expansion]]
- PRs: https://github.com/chakrits/AI-Agent-Workflow/pull/145 (Draft — implementation commit `0b67abd`, awaiting `status:spec-ready`/`status:development-done` label decision and QA verification before ready-for-review)
- Closeout PR: N/A (not yet merged)
- Postmortem: N/A

## Sub-tasks
- Enrichment: Decision Table Testing technique — [[../../.agents/skills/functional-test-design/SKILL.md]]
- Enrichment: State Transition Testing technique — same file
- Enrichment: Naming Convention rows `TC-DT-xxx`/`TC-ST-xxx` — same file
- Enrichment: Determinism + Explicit Test Data worked example (Output Quality Rules #6/#7) — same file
- Template update: `function-test-report.md` section 10 renamed "Test Design Techniques" + 2 new subsections — [[../../.agents/skills/functional-test-design/templates/function-test-report.md]]
- Template update: Coverage Matrix gains `Decision Table`/`State Transition` columns — same file
- Deferred (separate future Issue, not this one): TC-ID → automated-test traceability (`Automated`/`Test Ref` Coverage Matrix columns, `test-quality-discipline` checklist item)

## Provenance
- 2026-08-05 — Boss asked for QA Agent's current capabilities/philosophy; explained Skill Routing table, evidence-based reporting, Cross-Platform Acceptance Criteria Gate, and the no-fixed-threshold mutation-testing precedent.
- 2026-08-05 — Boss noted `functional-test-design` looked disproportionately large next to `test-quality-discipline`/`tdd-implementation` and asked for analysis on tightening the QA/tester discipline pipeline. Found a real gap: no skill or template anywhere cross-references a QA `TC-*` Test Case ID to an actual automated test — `functional-test-design`'s own Coverage/Traceability matrices track requirement→TC-ID and technique coverage, but nothing tracks TC-ID→automated-test-ref, and `tdd-implementation`/`qa-playwright-testing`/`test-quality-discipline` never mention TC-IDs at all.
- 2026-08-05 — Advisor review caught that an initial proposal (Developer cites TC-ID in `tdd-implementation`'s RED step) was incoherent given Developer Agent's existing Definition-of-Done Restatement rule (restates AC from BA's requirement record, not QA's test design) and the absence of any canonical phase pinning `functional-test-design` before `phase:development`. Revised to a QA-only fix: Coverage Matrix gains an `Automated`/`Test Ref` column QA fills in during verification. This fix was deferred to its own follow-up Issue (not yet opened) rather than bundled here.
- 2026-08-05 — Checked consistency against `AC_TRACEABILITY.md` (which declares itself "the only canonical AC matrix"), `TEST_REPORT.md`, `TEST_PLAN.md`, `REPRO_STEPS.md`, `HANDOFF.md`, `.github/ISSUE_TEMPLATE/work-item.md`, `.gitlab/merge_request_templates/Default.md`. Confirmed no naming/granularity collision (AC-ID vs TC-ID are different levels, different documents, different enforced gates) but flagged a pre-existing (not newly introduced) naming-overlap between `functional-test-design`'s own "Traceability Matrix" section name and `AC_TRACEABILITY.md`'s "Acceptance Traceability Matrix" — noted as out-of-scope for both this Issue and the deferred one.
- 2026-08-05 — Boss supplied an external `skill.md` QA agent guide example for screening. Screened section by section: adopted Decision Table Testing, State Transition Testing (both absent from the whole catalog, confirmed by grep, and directly relevant to the e-claim/insurance domain's claim-eligibility rule matrices and claim-status lifecycles), plus a Determinism/Explicit Test Data worked-example enrichment. Rejected Gherkin-as-default (conflicts with `qa-playwright-testing`'s existing deliberate opt-in design), the duplicate ASCII workflow diagram, the weaker qualitative Risk Score, the redundant Test Type column, the Role/Identity persona section, and the "always be exhaustive" directive (conflicts with this repo's own anti-scope-creep principle). An advisor pass caught two initially-listed adopt items (test-case order independence, "Dependency Failures" as a first-class row) were already covered or purely cosmetic — both dropped for consistency with the same cost/value bar applied to the rejected items.
- 2026-08-05 — Boss approved the 2-Issue split (this Issue first; TC-ID traceability follow-up after, to avoid a Coverage Matrix merge collision) and asked to see the implementation plan, which was written and this Issue opened per `.github/ISSUE_TEMPLATE/work-item.md`.

## Lessons Learned
- N/A yet — candidate lesson once closed: when an external "agent skill guide" reads as more authoritative-sounding than this repo's own accumulated design decisions (Gherkin opt-in, quantitative risk scoring, mode-based technique selection), the correct default is to check for an existing deliberate decision before adopting, not to assume newer/more detailed content is automatically better.

## Metrics
- Tests before: 379 (at PR #141 merge) → Tests after: TBD (regression tests not yet written)
- Subagent timeouts: 0
- Rework cycles: 0 so far
- Packet version: N/A (single-session, no cross-turn dispatch)

## Status: Open — plan and Issue created, implementation not yet started (branch `feature/functional-test-design-technique-expansion`)
