{
  "name": "ai-agent-dynamic-workflow",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "validate:contracts": "node scripts/validate-contracts.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.5.1"
  }
}
